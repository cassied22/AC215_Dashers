# PIPELINE DEFINITION
# Name: data-processor-pipeline
components:
  comp-data-processor-component:
    executorLabel: exec-data-processor-component
deploymentSpec:
  executors:
    exec-data-processor-component:
      container:
        args:
        - --executor_input
        - '{{$}}'
        - --function_to_execute
        - data_processor_component
        command:
        - sh
        - -c
        - "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip ||\
          \ python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1\
          \ python3 -m pip install --quiet --no-warn-script-location 'kfp==2.4.0'\
          \ '--no-deps' 'typing-extensions>=3.7.4,<5; python_version<\"3.9\"' && \"\
          $0\" \"$@\"\n"
        - sh
        - -ec
        - 'program_path=$(mktemp -d)


          printf "%s" "$0" > "$program_path/ephemeral_component.py"

          _KFP_RUNTIME=true python3 -m kfp.dsl.executor_main                         --component_module_path                         "$program_path/ephemeral_component.py"                         "$@"

          '
        - "\nimport kfp\nfrom kfp import dsl\nfrom kfp.dsl import *\nfrom typing import\
          \ *\n\ndef data_processor_component():\n    data_clean()\n    data_prepare()\n\
          \    data_upload()\n\n"
        image: python:3.7
pipelineInfo:
  name: data-processor-pipeline
root:
  dag:
    tasks:
      data-processor-component:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-data-processor-component
        taskInfo:
          name: data-processor-component
schemaVersion: 2.1.0
sdkVersion: kfp-2.4.0
