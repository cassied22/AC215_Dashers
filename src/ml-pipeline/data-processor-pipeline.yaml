name: data-processor
image: gcr.io/${GCP_PROJECT}/food-planner-data-processor:latest
command:
  - python
  - cli.py
  - --data_processor
env:
  - name: GCP_PROJECT
    value: ${GCP_PROJECT}
  - name: GCS_BUCKET_NAME 
    value: ${GCS_BUCKET_NAME}